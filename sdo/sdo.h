#error File is used for documentation

/**
 * \namespace sdo

 * \brief Namespace containing the interface to the parsers for mdl, voc, vpd and vop files
 * as well as the datastructures to represent their contents suitable for further
 * processing.
 * 
 * A vop file is represented by the class sdo::VopFile. Voc and mdl files are both represented
 * by the sdo::ExpressionGraph. And vpd files are represented by sdo::Objective.
 *
 * \file Parsers.hpp \brief File containing the parser interfaces
 * \file Symbol.hpp \brief Declaration of sdo::Symbol and specialization of std::hash for it.
 */
 